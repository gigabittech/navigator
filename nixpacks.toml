[phases.install]
cmds = ["corepack enable", "pnpm install --frozen-lockfile --prod=false"]

[phases.build]
cmds = ["pnpm --filter @navigator/web build"]

[start]
cmd = "pnpm --filter @navigator/web start -- --hostname 0.0.0.0 --port 3000"
